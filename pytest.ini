[pytest]
# Config at project root so pytest uses this dir as rootdir:
# .pytest_cache and temp dirs stay here, not in outputs/ or elsewhere.
testpaths = tests
cache_dir = .pytest_cache

markers =
    mfem: tests that require PyMFEM (skip with -m "not mfem")
